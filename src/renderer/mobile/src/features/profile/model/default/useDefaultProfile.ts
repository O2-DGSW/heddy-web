export const useDefaultProfile = () => {
  // const { data } = useFieldOfTopProfileQuery();
  const data = {
    name: "오용준",
    role: "디자이너",
    customer: 12,
    cutCnt: 12,
    cutData: 12,
  };

  return { data };
};
