import DropdownArrowIcon from "../assets/dropdown-arrow.svg?react";

const DropdownIcon = ({ className }: { className: string }) => (
  <DropdownArrowIcon aria-hidden="true" className={className} />
);

export { DropdownIcon };
